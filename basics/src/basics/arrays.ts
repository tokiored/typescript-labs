/**
 *
 * arrays
 *
 */
const users: string[] = ['Miles', 'Coltrain', 'Parker']
users.push('Dizzy')
/**
 *
 * type inference with arrays
 *
 */
const fruit = ['apples', 'pairs', 'oranges']
fruit.push('tomatoe')

const mixed = ['one', 1]
mixed.push('two')
mixed.push(2)
